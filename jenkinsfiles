pipeline {
    agent any

    stages {

        stage('Clone Repository') {
            steps {
                git 'https://github.com/cskarthi2752/car-website-1.git'
            }
        }

        stage('Deploy Website') {
            steps {
                sh '''
                sudo cp -r * /var/www/html/
                sudo rm -rf /var/www/html/.git
                sudo systemctl restart httpd
                '''
            }
        }

    }
}
